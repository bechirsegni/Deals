<%= form_for @category do |f| %>
    <%= f.text_field :icon, value:'icon_set_3_'%>
    <%= f.text_field :name%>
    <%= f.select :parent_id, nested_set_options(Category, @category) {|i| "#{'-' * i.level} #{i.name}" }, include_blank: true %>
    <%= f.submit %>
<% end %>